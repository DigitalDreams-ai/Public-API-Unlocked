# Trigger Field Values

Outbound webhook delivery is driven by the trigger field configured on `PublicApi_Integration_Config__c`:

- `Outbound_Trigger_Enabled__c = true` (or blank, treated as enabled)
- `Outbound_Trigger_Field_Name__c` identifies the watched field on `PublicApi_Submission__c`
- `Outbound_Trigger_Field_Values__c` lists the values that should send a webhook

Accepted `Outbound_Trigger_Field_Values__c` formats:

- comma-delimited text
- newline-delimited text
- semicolon-delimited text

Example:

- `Outbound_Trigger_Field_Name__c = Processing_Status__c`
- `Outbound_Trigger_Field_Values__c = Succeeded`

Built-in outbound uses package-managed submission state by default. You can still configure another `PublicApi_Submission__c` field if you need a custom trigger contract.

